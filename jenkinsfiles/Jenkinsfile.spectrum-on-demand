elifePipeline {
    stage 'Spectrum tests', {
        elifeEnd2endTest(
            null,
            params.marker ?: null,
            params.environment ?: 'end2end',
            6,
            params.revision ?: 'master',
            params.articleId ?: null
        )
    }
}
