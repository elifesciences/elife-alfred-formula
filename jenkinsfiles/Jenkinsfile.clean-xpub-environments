elifePipeline {
    def pullRequests
    stage "Calculate old pull requests", {
        checkout scm
        // ["902", ...]
        def oldPullRequest = jenkinsCli("groovy = <jenkinsfiles/old-pull-requests.groovy elife-xpub", true).trim().split("\n")
        echo "Found old pull requests: ${oldPullRequest}"
    }

    stage "Delete Helm releases", {
        // ["elife-xpub--pr-902", ...]
        def releases = sh(script: "sudo -u elife -H helm ls elife-xpub-- --output json | jq -r '.Releases[].Name'", returnStdout: true).trim().split("\n")
        for (pr in oldPullRequest) {
            println(pr) 
        }
    }
}
